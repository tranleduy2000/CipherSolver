<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="flynn.tim.ciphersolver">

    <application
        android:allowBackup="true"
        android:icon="@drawable/lock_light"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activities.MainActivity"
            android:label="Cipher Solver">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".caesar.CaesarCipherActivity"
            android:label="Caesar Cipher" />
        <activity
            android:name=".atbash.AtbashActivity"
            android:label="Atbash" />
        <activity
            android:name=".vigenere.VigenereCipherActivity"
            android:label="Vigenère Cipher" />
        <activity
            android:name=".frequency.FrequencyActivity"
            android:label="Frequency Analysis" />

        <activity
            android:name=".rot13.Rot13CipherActivity"
            android:label="Rot-13 Cipher" />
    </application>

</manifest>
